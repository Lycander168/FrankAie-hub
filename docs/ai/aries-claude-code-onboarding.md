# 電商營運 × Claude Code 上手指南

> 對象:電商營運(現任 Aries)|建立:2026-08-10|維護:營運管理
> 設計原則:**第一週只學三個,每個都對應你現在最花時間的工作。**

---

## 帳號:`ec@lycander.tw`(職務型帳號)

本帳號以 **`ec@lycander.tw`** 註冊,屬於**電商營運這個職務**,不屬於個人。

**這樣做的好處**:人員異動時,AI 的歷史對話、自訂範本、設定都留在職務上,
接手的人直接沿用,不必重新建立 —— 與員工手冊改職稱命名是同一個邏輯。

| 項目 | 說明 |
|---|---|
| 註冊信箱 | `ec@lycander.tw` |
| 方案 | Claude Pro(個人使用足夠)或 Team(需多人共用時) |
| 用途 | 電商營運:活動文案、商品上架、客服草稿、數據整理 |
| 申請人 | Aaron(依手冊 ERP 權限表,帳號與授權歸負責人) |
| 目前使用者 | Aries |

### ⚠️ 三項必要配套(請務必完成)

因為 `ec@` 同時正在辦理變更為 `Aries@`,若不處理下列項目,日後帳號會有復原風險:

**1. `ec@lycander.tw` 需長期保留,不可停用**

IT 申請信原本寫「別名保留 6 個月,屆時再評估停用」。
請改為:**`ec@` 作為 Claude 等系統帳號的註冊信箱,需長期保留收信功能**。
密碼重設與登入驗證信都會寄到這裡,停用即無法復原帳號。

**2. Claude 帳號設定備援驗證**

註冊完成後,立刻到帳號設定加入備援方式(擇一或並行):
- 綁定手機號碼
- 設定備援信箱(建議填 `Frankie@lycander.tw`)

這是最實在的保險 —— 即使信箱端出問題,仍能取回帳號。

**3. 帳號資訊登錄**

將帳號、方案、開通日填入員工工作手冊「ERP權限表」分頁下方的帳號清單,
比照正航帳號管理。人員異動時,一併辦理密碼變更。

---

## 一、30 分鐘完成設定

### 步驟 1|安裝 Claude Code(選最簡單的方式)

**推薦:桌面版 App**(Mac / Windows)

到 [claude.com/code](https://claude.com/code) 下載安裝，一路下一步即可。

> 也可以直接用瀏覽器版 [claude.ai/code](https://claude.ai/code)，不必安裝任何東西。
> 終端機 CLI 版是給工程師用的，你不需要。

### 步驟 2|登入

用 `ec@lycander.tw` 登入(帳號由 Aaron 開通後提供密碼)。

### 步驟 3|安裝公司的 AI 團隊 plugin

在 Claude Code 的輸入框直接輸入這兩行(一次一行,等第一行跑完再輸第二行):

```
/plugin marketplace add Lycander168/FrankAie-hub
```

```
/plugin install frankaie-research-hub@frankaie-hub
```

> 若出現 GitHub 權限問題,找 Frankie 協助。

### 步驟 4|確認安裝成功

輸入:

```
/help
```

看到 skill 清單中有 `listing-optimizer`、`ad-hook-writer` 等項目就成功了。

### 步驟 5|試跑第一個

輸入:

```
/ad-hook-writer 幫我為 PQI 65W 快充寫廣告 hook,主打輕巧與快充,受眾是常出差的上班族
```

跑得出東西就代表設定完成,可以開始用了。

---

## 二、第一週:只學這三個

不要一次看完 33 個 skill。**先把這三個用熟,其他之後再說。**

這三個是一條線:想 hook → 寫商品頁 → 潤稿,正好是你做活動最花時間的部分。

### 1️⃣ `/ad-hook-writer` — 廣告文案 hook

**用在**:8/25 檔期的廣告素材、社群貼文開場、短影音腳本

**怎麼用**:

```
/ad-hook-writer 產品:PQI 65W 氮化鎵充電器
賣點:比原廠小一半、可同時充筆電+手機
受眾:常出差的上班族
用途:蝦皮活動主圖文案
```

**會產出**:8 種不同框架的 hook + 短影音開場分鏡

**省下的時間**:原本想文案卡住半小時,現在 3 分鐘拿到 8 個方向,挑一個改。

---

### 2️⃣ `/listing-optimizer` — 商品頁文案

**用在**:官網新品上架、蝦皮商品頁優化

**怎麼用**:

```
/listing-optimizer 幫我優化這個商品的蝦皮頁面
產品:(貼上規格)
目前標題:(貼上現有標題)
主要關鍵字:氮化鎵充電器、65W快充
```

**會產出**:轉換導向的標題、五點特色、賣點結構、後台關鍵字

**省下的時間**:上架一個商品的文案,從 30–40 分鐘縮到 10 分鐘。

---

### 3️⃣ `/copy-humanizer` — 文案去 AI 味

**用在**:任何 AI 產出的文案,發布前都先過一次

**怎麼用**:

```
/copy-humanizer (貼上你要潤的文案)
語氣:親切、口語,像跟朋友推薦
```

**會產出**:改自然的版本 + 改了哪些的清單(事實不會被改動)

**為什麼重要**:AI 寫的文案有固定腔調,顧客看得出來。這一步是讓文案「像人寫的」的關鍵。

---

## 三、第二週起:依工作項目擴充

熟悉上面三個之後,再依當下需要挑用。**不用背,遇到再查這張表。**

| 你要做的事 | 用哪個 |
|---|---|
| 分析商品評價找痛點與賣點 | `/review-miner` |
| 想活動關鍵字、SEO 字詞 | `/seo-keyword-expander` |
| 寫客服回覆、處理客訴話術 | `/cx-team` |
| 做 EDM、會員信序列 | `/email-sequence-builder` |
| 比較競品、找差異化說法 | `/competitor-comparison` |
| 算活動折扣後的毛利 | `/pricing-calculator` |
| 整理銷售數據、找掉量原因 | `/data-analyst` |
| 提升回購率、會員經營 | `/growth-retention` |
| 完整活動企劃(要多角色分工) | `/marketing-team` |
| 想測文案或定價會不會被買 | `/consumers-taiwan` |
| **不知道該找誰** | `/hub-router` |

> `/hub-router` 是總機。任何時候卡住、不知道用哪個,就丟給它,它會告訴你該找誰、順序怎麼排。

---

## 四、使用規範(重要)

### 🔴 不要輸入的內容

| 不可輸入 | 原因與替代做法 |
|---|---|
| 顧客個資(姓名、電話、地址、訂單編號) | 個資保護。要分析客訴時,把個資去掉再貼 |
| 供應商報價、成本結構 | 商業機密。需要算毛利時用 `/pricing-calculator` 輸入區間或代號 |
| 客戶的規範文件原檔 | 例如台灣大哥大的標籤規範,以公司規範表為準,不要靠 AI 記憶 |

### ✅ 產出一定要人工確認的三件事

1. **規格數字**(W 數、尺寸、容量)— AI 可能寫錯,對照原始規格表
2. **價格與折扣** — 一律回到正航或官網後台確認
3. **客戶規範相關內容**(標籤、包裝、外箱標示)— 以書面規範表為準

> 這一條是有原因的:先前 PQI 65W 因標籤不符客戶規範被退貨,15,000 pcs 全數重貼。
> AI 產出的文案同樣可能與規範不符,**它不會知道客戶的最新規範**。

### 💡 使用原則

- AI 是產草稿的工具,不是決策者。決定要不要用、怎麼改,還是你
- 覺得產出不好,就把不滿意的地方講出來讓它改,不必自己從頭重寫
- 用得順的指令,存成自己的範本,下次直接改參數

---

## 五、遇到問題找誰

| 問題類型 | 找誰 |
|---|---|
| 帳號、訂閱、授權 | Aaron |
| 安裝、plugin、怎麼用 | Frankie |
| 某個 skill 產出不理想 | Frankie(可調整 skill 內容) |

---

## 六、上手節奏建議

考量目前工作量與 8/25 檔期,建議節奏:

| 時間 | 做什麼 | 預計投入 |
|---|---|---|
| 第 1 天 | 完成設定(第一段) | 30 分鐘 |
| 第 1 週 | 用 `/ad-hook-writer` 產 8/25 活動文案 | 邊做邊學,不額外花時間 |
| 第 2 週 | 加入 `/listing-optimizer` 與 `/copy-humanizer` | 用在既有上架工作上 |
| 8/25 後 | 依第三段的表,挑當下需要的擴充 | 不設進度 |

> **這份工具是為了讓你的工作變少,不是多一件事要學。**
> 如果學習本身變成負擔,就先停下來,只用第一個 skill 就好,其他等檔期過了再說。
